[package]
name = "substrate-sel4-host-tests"
version = "0.1.0"
edition = "2024"
publish = false

# The seL4 runtime cannot link on the host; test the pure module in isolation.
[workspace]

[lib]
path = "../src/free_slots.rs"
doctest = false
